*.iml
.gradle
.DS_Store
/local.properties
/.idea
/build
build/
/captures
.externalNativeBuild
# Native binaries + shared libs populated by ../apertium-native/install-to-app.sh
app/src/main/jniLibs/
# Legacy flat layout, now replaced by pair_<code>/src/main/assets/ packs.
app/src/main/assets/pairs/
# Upload keystore is CI-only (decoded from PLAY_UPLOAD_KEYSTORE_BASE64 secret).
app/upload.keystore
# Java heap-dump snapshots from OOM diagnostic runs.
*.hprof
# Flat staging dir populated by apertium-native/prep-pair.sh on dev/CI.
pair-jars/
# Python bytecode from scripts/*.py
__pycache__/